#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

//#define max(a,b) a > b ? a : b // 1
//#define max3(a,b,c) max(max(a,b), c) // 2
//#define max4(a,b,c,d) max(max(max(a,b), c), d) // 3

int main() {
	//printf("max(a,b) = %d", max(17, 25)); // 1
	//printf("max(a,b,c) = %d", max3(17, 25, 50)); // 2
	//printf("max(a,b,c,d) = %d", max4(17, 25, 50, 55)); // 3

	return 0;
}